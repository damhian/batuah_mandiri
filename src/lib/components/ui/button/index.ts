import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./button.svelte";

export {
	Root,
	type ButtonProps as Props,
	//
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};

export { default as Button } from "./button.svelte";
